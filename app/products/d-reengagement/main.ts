/* Product D — Member Re-engagement Tool. Rensley's lane: only Rensley (and
   Rensley's AI) edits files in this folder.
   Shared types come from ../../shared/contract.js and shared records load
   through loadFixtures() in ../../shared/data.js — never copy shared data
   into this folder. Build with `npm run build` from the repo root.
   Law from SHARED_DATA_CONTRACT.md: this product drafts outreach messages
   for staff review only — nothing here ever sends a message. */

export {};
